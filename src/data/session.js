export default class Session {

	// command history

	// loaded plugins / agents

	// agents config ? (prompt, instruct, ...)

	constructor(ctx) {
		this.ctx = ctx
	}

	static load(file) {

	}

	save() {

	}
}
